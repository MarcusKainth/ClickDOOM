#!/usr/bin/env bash
# Differential run: execute N instructions of the pinned ROM on refemu and
# sqlcpu, compare SPEC §7 checkpoint traces, report the first divergence in
# divergence-report form fields.
#
# STUB — implemented by the executor workstream (see task backlog).
set -euo pipefail
echo "::error::scripts/diff_run.sh is a stub. Implement per SPEC §7 (executor workstream)." >&2
exit 1
